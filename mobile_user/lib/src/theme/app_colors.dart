import 'package:flutter/material.dart';

class AppColors {
  /// Lighter green from the FlowBike logo
  static const Color primaryLight = Color.fromARGB(255, 47, 158, 56);

  /// Darker green from the FlowBike logo
  static const Color primaryDark = Color.fromARGB(255, 31, 77, 48);
}
